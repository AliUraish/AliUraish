# Ali Uraish — Portfolio

Single static page. No build step, no JavaScript, no dependencies.

- `index.html` — all content (intro, timeline, contact)
- `styles.css` — black & white styling
- `assets/favicon.png`

Run locally: `npx serve .` or `python3 -m http.server 3000`

Deploy: `vercel --prod`
